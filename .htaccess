RewriteEngine On

# Rediriger /meilleur-disque-menstruel-comparatif vers / (page d'accueil)
RewriteRule ^meilleur-disque-menstruel-comparatif$ / [R=301,L]

# Rediriger les URLs .html vers la version sans extension (SEO : évite le contenu dupliqué)
RewriteCond %{THE_REQUEST} \s/([^.]+)\.html[\s?] [NC]
RewriteRule ^ /%1 [R=301,L]

# Servir le fichier .html correspondant si l'URL est sans extension
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L]
